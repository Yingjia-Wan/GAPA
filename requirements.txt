# Pinned to the environment that produced the published results (Python 3.10).
# Install the repo itself first so `import gapa` works from any directory:
#     pip install -e .
#     pip install -r requirements.txt

# --- core analysis ---
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
statsmodels==0.14.6
scikit-learn==1.7.2

# --- figures ---
matplotlib==3.10.8
seaborn==0.13.2
pillow==12.0.0

# --- predictor training / evaluation (section 6.1) ---
# torch 2.10.0+cu130 was used; the local CUDA build is machine-specific, so
# install torch for your own CUDA version first (https://pytorch.org/get-started).
torch>=2.4
transformers==4.57.3
peft==0.18.0
datasets==2.21.0
accelerate==1.12.0
safetensors==0.7.0
torchmetrics==1.5.2
sentencepiece==0.2.1
huggingface-hub==0.36.2
wandb==0.23.1

# --- zero-shot LLM evaluation (section 5); API keys go in .env ---
openai==2.21.0
google-genai==1.64.0
anthropic            # not installed in the reference env; needed for Claude models

# --- attribute extraction (section 3) ---
nltk==3.9.3
pydantic==2.12.5

# --- inter-rater reliability (data/krippendorff_alpha/) ---
krippendorff         # not installed in the reference env; that script is optional

# --- misc ---
python-dotenv==1.2.1
requests==2.32.5
tqdm==4.67.3
